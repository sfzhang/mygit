# git username and password
# !@# would be replaced by %21%40%23

usl = https://user:passwd@repository

[credential "https://github.com"]
    username = sfzhang

# credential.helper "cache --timeout"
git config credential.helper "cache --timeout=X"
git config credential.helper "store --store=path"
